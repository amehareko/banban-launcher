# 📔 更新记录

都用语义化版本号，装新包前瞄一眼就知道改了啥 (๑•̀ㅂ•́)و✧

---

## 1.6.0

**新增：隐私子系统**

- 长按列表里的应用 → 二次确认后隐藏；隐藏后 5 秒内可一键「撤销」
- 长按右下角 ▦ 按钮 → **内置数字键盘**输密码 → 进入「管理模式」，
  隐藏项半透明显示并带「已隐藏」角标，长按可恢复；关掉列表自动退出管理模式
- 密码 4~8 位，可在**设置 → 隐私**里开关 / 修改（验旧密码 + 二次确认）
- 忘记密码自救：设置 → 隐私 → 长按「修改」3 秒即可清除，隐藏列表不受影响
- 「隐藏需密码」可选开关（**默认关闭**，保持免密长按即藏）

**新增：设置面板分类**

- 顶栏按钮「主题」改为「设置」，内分三类：**主题 / 文字 / 隐私**，各自独立滚动

**改进**

- 返回键：有弹层时关掉最上层（密码 > 确认 > 一言 > 天气 > 设置 > 应用列表），
  没有弹层时保持常驻不退出
- 弹层状态由原生每 500ms 主动查询（不再赌 scheme 回调），退到后台停止轮询

**修复**

- 存储写不进去时不再「假成功」（原先提示已隐藏、刷新后应用又回来）
- 应用卸载后残留的隐藏项会自动清理，但**只在列表完整时才清**，避免误清
- 设置面板切换分类后滚动位置回顶

**工程**

- 新增 `tools/es5-check.js`：出包前扫 ES6 语法，防止老内核白屏
- 冒烟测试扩到 **116 项**

---

## 1.5.0

- 长按一言卡片可选类型：上游 `v1.hitokoto.cn` 的 a~l 全 12 类 + 全部随机
- 修复：切换类型时若在途请求未回，刷新会被静默丢弃

## 1.4.4

- 修复滚动列表误触（第二轮）：`touchmove` 在合成器接管后不保证送达，
  改用 scrollTop 快照 + 时长 + 延迟执行判定
- 长按上限 600ms → 300ms，判定后延迟 140ms 执行

## 1.4.3

- 修复滚动列表误触（第一轮）：自实现 tap 判定替代裸 `click`

## 1.4.2

- 长按天气卡片可改位置：城市名自动地理编码，持久化保存

## 1.4.1

- 修复部分设备上 `addJavascriptInterface` 注入异常导致「未检测到应用环境」
  → 双通道兜底：`evaluateJavascript` 注入数据 + `banbanx://launch` scheme 启动
- APK 文件名改 ASCII（中文名会让 adb install 报 `remote Is a directory`）

## 1.4.0

- 定稿 Crosswalk 23 内嵌内核单版本（放弃 WebViewUpgrade Hook 方案）
- 新增右下角应用启动器
